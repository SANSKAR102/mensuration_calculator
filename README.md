# Mensuration Calculator in C

This is a simple command-line calculator written in C that calculates the area and perimeter/circumference of basic geometric shapes: **Circle**, **Rectangle**, and **Triangle**.

# Features

- Circle: Area and Circumference
- Rectangle: Area and Perimeter
- Triangle: Area (Heron’s formula) and Perimeter
- Input validation
- Simple menu-driven interface

# Compilation

Use `gcc` to compile:

```bash
gcc mensuration_calculator.c -o calculator -lm
